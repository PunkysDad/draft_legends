import { View, Text } from 'react-native';

export default function DraftScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor: '#000' }}>
      <Text style={{ color: '#fff', fontSize: 18 }}>Draft Room</Text>
    </View>
  );
}
